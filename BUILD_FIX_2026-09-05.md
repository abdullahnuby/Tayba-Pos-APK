# Build fix — 2026-09-05

Fixed the TypeScript build error in `src/components/sections/sales-section.tsx`.

The `/api/register-sessions` response includes `cashSales`, `cardSales`, and `transferSales`, but the React Query type only declared `id`, `status`, and `openingFloat`. The local type has been expanded to match the actual API response shape, including register totals and report metadata.

This is a type-contract fix only; no runtime business behavior was changed.
