# تشغيل وتجهيز Tayba POS كبرنامج Desktop

البرنامج اتحول بنجاح لتطبيق Electron. اتعمل واتجرب هنا (Linux build نجح 100%). عشان تطلع نسخة Windows (.exe) لازم تشغّل الأوامر دي على جهاز Windows بتاعك (الشركات زي مايكروسوفت مش بتسمح ببناء exe موقّع من على لينكس بسهولة، فالأفضل تبنيها من نفس نظام التشغيل المستهدف).

## المتطلبات على جهاز Windows
1. تثبيت [Node.js](https://nodejs.org) (نسخة 20 أو أحدث)
2. فك ضغط المشروع اللي هبعتهولك

## خطوات البناء (مرة واحدة بس، من CMD أو PowerShell جوه فولدر المشروع)

```
npm install
npm run electron:build:win
```

هيطلعلك ملف `.exe` (installer) جوه فولدر `release/`. ده تقدر تنسخه لأي جهاز كاشير وتنصبه عادي زي أي برنامج ويندوز.

## تجربة سريعة قبل عمل installer (اختياري)
```
npm run electron:dev
```
ده بيفتح نافذة desktop فيها البرنامج فورًا من غير ما يعمل installer، مفيد للتجربة بسرعة.

## أوامر مفيدة تانية
- `npm run electron:build:linux` → لو حبيت تجربه على لينكس (AppImage)
- `npm run electron:build:mac` → لو حبيت تجربه على ماك (لازم يتعمل من جهاز ماك)

## ملاحظات مهمة
- البرنامج شغال Offline بالكامل زي نسخة الأندرويد بالظبط — بيستخدم SQLite (sql.js) وتخزين محلي (IndexedDB)، مفيش سيرفر ولا إنترنت مطلوب.
- كل قاعدة البيانات والبيانات بتتخزن جوه نفس الجهاز اللي البرنامج شغال عليه — يعني نسخة الويندوز هتبقى منفصلة عن نسخة الأندرويد وبياناتهم مش متزامنة أوتوماتيك (لو محتاج مزامنة بين الجهازين قولّي نظبطها).
- تقدر تغيّر أيقونة البرنامج بتحديث `resources/icon.png` قبل البناء.
- أول مرة تعمل `electron:build:win` هينزّل نسخة Electron من GitHub (حوالي 100+ ميجا) فمحتاج إنترنت.

---

## نظام التفعيل (تسجيل دخول صاحب النظام)

البرنامج **معدش يفتح على أي جهاز إلا لو انت دخلت بإيميلك وباسوردك عليه بنفسك مرة واحدة.**

### قبل ما تبني نسخة للتوزيع — لازم تعمل الخطوة دي
افتح ملف `electron/license.cjs` وغيّر السطرين دول لإيميل وباسورد من اختيارك انت:
```js
const OWNER_EMAIL = 'abdullah@tayba-eg.com'      // <-- غيّرها لإيميلك
const OWNER_PASSWORD = 'Tayba@Luxor2026!'        // <-- غيّرها لباسورد قوي
```
لو سبتهم زي ما هم، أي حد يعرف القيم دي (لو شافها في الكود) هيقدر يفعّل البرنامج. خليهم سر بينك وبين نفسك.

### إزاي بيشتغل عند العميل
1. تسطّب البرنامج على جهاز العميل
2. أول ما يفتح، هتظهر شاشة "تفعيل نظام طيبة" بتطلب إيميل وباسورد
3. انت (مش العميل) تدخل بإيميلك وباسوردك اللي حطيتهم فوق
4. البرنامج بيولّد كود تفعيل خاص بالجهاز ده تلقائي ويحفظه محليًا، ومايطلبش تاني على نفس الجهاز
5. لو الجهاز اتمسح أو البرنامج اتنصب تاني، تدخل بنفس الإيميل والباسورد وهيرجع يشتغل فورًا (الكود بيتولد تلقائي من جديد بنفس القيمة، مش عشوائي)

### مهم جدًا
- الشاشة دي بتاعتك انت بس، مش للعميل — العميل عادي بيدخل بحسابه هو (كاشير/مدير) بعد التفعيل ده، زي أي وقت
- محدش يقدر يفعّل نسخة على أي جهاز من غير ما يعرف إيميلك وباسوردك
- ملف `electron/license.cjs` فيه السر ده — **متوزّعوش لحد**، ولو هتبعت المشروع لمبرمج تاني يعدّل حاجة، امسح الملف ده الأول أو غيّر القيم بعد كده
