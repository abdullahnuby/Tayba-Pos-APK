# Tayba POS — Device Integration Sprint

## الهدف
تعريف الأجهزة وربطها بنقطة البيع على Windows Desktop بدون كشف Node.js للواجهة.

## الأجهزة المدعومة في هذا الإصدار
- قارئ باركود USB HID Keyboard Wedge أو Bluetooth HID Keyboard.
- طابعات Windows المثبتة: اختيار طابعة إيصال حراري وطابعة A4.
- درج نقدية متصل بالطابعة: نبضة ESC/POS عبر Windows spooler.

## ما تم ربطه فعليًا
- اكتشاف طابعات Windows من داخل التطبيق.
- حفظ تعريف الأجهزة في جدول `settings` بدون تغيير schema.
- اختبار طابعة الإيصال واختبار A4 من خلال Electron للطابعة المحددة.
- زر اختبار فتح درج النقدية.
- فتح درج النقدية تلقائيًا بعد الفاتورة النقدية عند وجود طابعة درج معرفة.
- إرسال الفاتورة المطبوعة إلى الطابعة المحددة بدل Browser Print في نافذة الفاتورة الجديدة، مع fallback إلى `window.print()`.
- اختبار قارئ الباركود داخل الإعدادات، مع إعادة استخدام input/search الموجود في POS للـHID.

## حدود المرحلة
- USB HID لا يحتاج إلى قراءة USB Raw من التطبيق؛ الجهاز يعمل كلوحة مفاتيح.
- فتح الدرج الخام مدعوم حاليًا على Windows/Electron لأن التحكم بـWinSpooler يحتاج native spooler APIs.
- لم يتم ادعاء دعم Bluetooth Serial/USB Serial Raw Scanner في هذه المرحلة.
- ما زال يلزم اختبار فعلي على أجهزة المحل، خصوصًا طابعة 58/80mm والدرج المتصل بها.
